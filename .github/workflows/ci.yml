name: PicdexerCI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Go
        uses: actions/setup-go@v2
      - name: Environment dependencies
        run: |
          apt-get -q -y install libimage-exiftool-perl imagemagick
          go version
          go install -v github.com/rakyll/statik
          go get -v
      - name: Go generate
        run: go generate ./...
      - name: Tests
        run: go test ./...